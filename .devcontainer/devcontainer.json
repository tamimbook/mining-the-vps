{
  "name": "Mining VPS with RDP & SKLauncher",
  "build": {
    "dockerfile": "Dockerfile",
    "context": "."
  },
  "runArgs": [
    "--cap-add=SYS_ADMIN",
    "--memory=128g",
    "--memory-swap=128g",
    "--shm-size=2g",
    "--privileged"
  ],
  "mounts": [
    "source=/workspaces,target=/workspaces,type=bind,consistency=cached"
  ],
  "remoteUser": "mining-the-vps-9qtajn0flxdl",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {
      "installGit": true,
      "installGpg": true
    }
  },
  "postCreateCommand": "sudo chmod +x /workspaces/lapdev_heal.sh && sudo /workspaces/lapdev_heal.sh && sudo chmod +x /workspaces/fastfetch_installation.sh && sudo /workspaces/fastfetch_installation.sh",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-vscode-remote.remote-containers"
      ]
    }
  }
}
